{
	"C++ Utility Snippet": {
		"prefix": "cp",
		"body": [
			"// clang-format off",
			"#include <bits/stdc++.h>",
			"#include <chrono>",
			"#include <ext/pb_ds/assoc_container.hpp> ",
			"#include <ext/pb_ds/tree_policy.hpp>",
			"",
			"using namespace __gnu_pbds;",
			"using namespace std;",
			"",
			"#define dbg(...) debug_out(#__VA_ARGS__, __VA_ARGS__)",
			"#define endl \"\\n\"",
			"#define MOD 1000000007",
			"#define MOD2 998244353",
			"#define largePrime 999999999999999989",
			"#define infinity 1e18",
			"#define rinfinity -1e18",
			"#define int long long",
			"#define ll long long ",
			"#define vi vector<int>",
			"#define vd vector<double>",
			"#define pii pair<int, int>",
			"#define vpi vector<pii>",
			"#define ld long double",
			"#define db double",
			"#define lsb(a) __builtin_ffsll(a)",
			"#define setBits(a) __builtin_popcountll(a)",
			"#define mostSetBit(a) ((a) == 0 ? -1 : (63 - __builtin_clzll(a)))",
			"#define all(a) a.begin(), a.end()",
			"#define fast ios_base::sync_with_stdio(false); cin.tie(NULL);",
			"#define no cout << \"NO\" << endl;",
			"#define yes cout << \"YES\" << endl;",
			"#define rep(i, begin, end) for (__typeof(end) i = (begin) - ((begin) > (end)); i != (end) - ((begin) > (end)); i += 1 - 2 * ((begin) > (end)))",
			"template<typename T> using ordered_set  = tree<T, null_type,less<T>, rb_tree_tag,tree_order_statistics_node_update>;",
			"template<typename T> using ordered_multiset = tree<T, null_type, less_equal<T>, rb_tree_tag, tree_order_statistics_node_update>;",
			"template <typename T> void read(vector<T> &arr) { for (T &elem : arr) cin >> elem; }",
			"",
			"template <typename T, typename K> ostream &operator<<(ostream &obj, const pair<T, K> &p) { return obj << p.first << \" \" << p.second ; }",
			"template <typename T> ostream &operator<<(ostream &obj, const vector<T> &arr) { for (const auto &elem : arr) obj << elem << \" \"; obj << endl; return obj; }",
			"template <typename T> ostream &operator<<(ostream &obj, const vector<vector<T>> &arr) { for (const auto &row : arr) { for (const auto &elem : row) obj << elem << \" \"; obj << endl; } obj << endl; return obj; }",
			"template <typename T> ostream &operator<<(ostream &obj, const vector<vector<vector<T>>> &arr) { int maxi = 0; for (const auto &mat : arr) maxi = max(maxi, (long long)mat.size()); for (int row = 0; row < maxi; ++row) { for (const auto &mat : arr) {if (row < mat.size()) { obj << \"[ \"; for (const auto &elem : mat[row]) obj << elem << \" \"; obj << \"] \"; } else obj << string(mat[0].size() * 3, ' '); } obj << endl; } obj << endl; return obj;}",
			"template <typename T, typename Compare = less<T>> ostream &operator<<(ostream &obj, const set<T, Compare> &arr) { for (const auto &elem : arr) obj << elem << \" \"; obj << endl; return obj; }",
			"template <typename T, typename Compare = less<T>> ostream &operator<<(ostream &obj, const multiset<T, Compare> &arr) { for (const auto &elem : arr) obj << elem << \" \"; obj << endl; return obj; }",
			"template <typename T, typename K, typename Compare = less<T>> ostream &operator<<(ostream &obj, const map<T, K, Compare> &arr) { for (const auto &[key, value] : arr) obj << key << \": \" << value << endl; obj << endl; return obj; }",
			"template <typename T, typename K, typename Compare = less<T>> ostream &operator<<(ostream &obj, const unordered_map<T, K, Compare> &arr) { for (const auto &[key, value] : arr) obj << key << \": \" << value << endl; obj << endl; return obj; }",
			"template <typename T, int N> typename enable_if<!is_same<T, char>::value, ostream &>::type operator<<(ostream &os, const T (&arr)[N]) { for (int i = 0; i < N; ++i) os << arr[i] << \" \"; return os; }",
			"template <typename T, int R, int C> ostream &operator<<(ostream &os, const T (&arr)[R][C]) { for (int i = 0; i < R; ++i) { for (int j = 0; j < C; ++j) os << arr[i][j] << \" \"; os << endl; } return os; }",
			"template <typename T, int N> ostream &__print(ostream &os, const T (&arr)[N], int rows = N) { rows = min(rows, N); for (int i = 0; i < rows; ++i) os << arr[i] << \" \"; os << endl; return os; }",
			"template <typename T, int R, int C> ostream &__print(ostream &os, const T (&arr)[R][C], int rows = R, int cols = C){ rows = min(rows, R); cols = min(cols, C); for (int i = 0; i < rows; ++i){ for (int j = 0; j < cols; ++j) os << arr[i][j] << \" \"; os << \"\\n\"; } return os; }",
			"template <typename T, int D, int R, int C> ostream &operator<<(ostream &os, const T (&arr)[D][R][C]) { for (int d = 0; d < D; ++d) { os << \"Layer \" << d << \":\\n\"; for (int r = 0; r < R; ++r) { for (int c = 0; c < C; ++c) os << arr[d][r][c] << \" \"; os << endl; } os << endl; } return os; }",
			"template <typename Arg1> void debug_out(const char *name, Arg1 &&arg1){ while (*name == ' ') name++; cerr << name << \"=\" << arg1 << \"\\n\"; }",
			"template <typename Arg1, typename... Args> void debug_out(const char *names, Arg1 &&arg1, Args &&...args){ const char *comma = strchr(names, ','); int len = comma - names; while (len > 0 && *names == ' ') { names++; len--; } cerr.write(names, len) << \"=\" << arg1 << \" \"; debug_out(comma + 1, args...); }",
			"bool acctosecond(const pii &a, const pii &b) { return a.second < b.second; }",
			"int get_hash(const string &s, int hashvalue = 31, int mod = 9999999967) { int val = 0; for (char ch : s) val = (val * hashvalue + (ch - 'a' + 1)) % mod; return val; }",
			"// clang-format on",
			"",
			"int multipleTestCases = 1;",
			"void solve(){",
			"    $2",
			"}",
			"",
			"int32_t main() {",
			"    fast",
			"    auto start = chrono::high_resolution_clock::now();",
			"",
			"    if (multipleTestCases)",
			"    {",
			"        int t;",
			"        cin >> t;",
			"        while (t--)",
			"        {",
			"            solve();",
			"        }",
			"    }",
			"    else",
			"    {",
			"        solve();",
			"    }",
			"",
			"    auto end = chrono::high_resolution_clock::now();",
			"    chrono::duration<double> duration = end - start;",
			"    // cout << \"Execution time: \" << duration.count() << \" seconds\" << endl;",
			"    return 0;",
			"}"
		],
		"description": "Insert a complete C++ utility code with execution timer and compact functions"
	}
}
