{
	"String Algorithms": {
		"scope": "cpp",
		"prefix": "AdvString",
		"body": [
			"// This algorithm calculates longest palindromic substring with a given center in O(1) space and time. Preprocessing takes O(N) time and space",
			"// Also tells whether a given [l...r] is palindrome or not in O(1) time and space",
			"class Manacher",
			"{",
			"private:",
			"    vector<int> p;",
			"    int maxLen;",
			"    string generateManacherString(string &s)",
			"    {",
			"        string ans = \"\";",
			"        int n = s.length();",
			"        for (int i = 0; i < n; i++)",
			"        {",
			"            ans.push_back('#');",
			"            ans.push_back(s[i]);",
			"        }",
			"        ans.push_back('#');",
			"        return ans;",
			"    }",
			"    void fillP(string &s)",
			"    {",
			"        string ms = generateManacherString(s);",
			"        int l = 0;",
			"        int r = 0;",
			"        int n = ms.length();",
			"        this->maxLen = min(1ll, (int)s.length()) - 1;",
			"        for (int i = 0; i < n; i++)",
			"        {",
			"            if (i > r)",
			"            {",
			"                l = i;",
			"                r = i;",
			"            }",
			"            int mirrorIdx = r + l - i;",
			"            int maxPossible = max(0ll, min(r - i + 1, p[mirrorIdx]));",
			"            p[i] = maxPossible;",
			"            if (i + maxPossible < r || mirrorIdx - p[mirrorIdx] + 1 < l)",
			"                continue;",
			"            int maxLen = p[i];",
			"            while (i >= maxLen && i + maxLen < n &&",
			"                   ms[i + maxLen] == ms[i - maxLen])",
			"            {",
			"                maxLen++;",
			"            }",
			"            p[i] = maxLen;",
			"            r = i + p[i] - 1;",
			"            l = i - p[i] + 1;",
			"            this->maxLen = max(this->maxLen, p[i]) - 1;",
			"        }",
			"    }",
			"",
			"public:",
			"    Manacher(string &inp)",
			"    {",
			"        int n = inp.size();",
			"        p.assign(2 * n + 1, 0);",
			"        fillP(inp);",
			"    }",
			"",
			"    int longestPalindrome(int cen, int odd)",
			"    {",
			"        int pos = 2 * cen + 1 + !odd;",
			"        return p[pos] - 1;",
			"    }",
			"",
			"    bool isPalindrome(int l, int r)",
			"    {",
			"        int len = r - l + 1;",
			"        int mid = (l + r) / 2;",
			"        return len <= longestPalindrome(mid, len % 2);",
			"    }",
			"};"
		]
	}
}
